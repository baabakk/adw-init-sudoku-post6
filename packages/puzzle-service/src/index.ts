// puzzle-service — Puzzle Service
// Scope: Node.js HTTP service that generates valid, uniquely-solvable Sudoku puzzles per difficulty and validates submitted boards. Exposes GET /puzzle and POST /validate endpoints. Uses SQLite for persistence (or in-memory cache).
// Owns: packages/puzzle-service
// This team builds its slice here each phase.
export {};
